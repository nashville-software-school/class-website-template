name: Deploy
on:
  push:
    branches:
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm install
    - name: Build Code
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
      run: npm run deploy
